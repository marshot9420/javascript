# JavaScript

JavaScript 공부

## 목표 정의

> 임의의 코드를 보았을 때 다음 과정을 자동으로 재구성할 수 있는 상태

1. 소스가 어떤 문법 구조로 파싱되는가
2. 실행 전에 어떤 선언과 스코프가 준비되는가
3. 각 표현식이 어떤 값으로 평가되는가
4. 함수 호출로 어떤 실행 컨텍스트가 생성되는가
5. 객체와 클로저가 무엇을 참조하고 있는가
6. 동기 코드와 비동기 작업이 어떤 순서로 실행되는가
7. 브라우저나 Node.js가 어디서 개입하는가
8. 엔진이 코드를 어떻게 해석·최적화하며 메모리를 회수하는가
9. 시간·공간 복잡도와 병목이 어디서 발생하는가

JavaScript라는 하나의 덩어리가 아니라 언어 명세, 호스트 환경, 엔진 구현, 컴퓨터 과학, 소프트웨어 설계라는 서로 다른 층으로 분리해야 한다.

ECMAScript 명세는 언어의 계산 규칙을 정의하지만 입출력, 타이머, DOM, 파일 시스템 같은 기능을 직접 정의하지 않는다. 이런 기능과 실행 스케줄링은 브라우저나 Node.js 같은 호스트가 담당한다.

## 문서

> 문서는 최대한 세세한 부분까지 작성되어 있으나, 이는 어디까지나 참고용일뿐이다. 전체적인 맥락을 따라가되 문서 내용에 억지로 맞추어 진행하지 않도록 주의한다.

- [1. 분류](./docs/01_분류.md)
- [2. 순환](./docs/02_순환.md)
- [3. 계획](./docs/03_계획.md)

## 구조

```
javascript
├─ README.md
├─ logs/
├─ experiments/
├─ explanations/
├─ index.html
├─ main.js
├─ projects/
├─ questions/
└─ trainings/
```

- `logs/`: 공부 내용을 기록하는 곳이다.
- `index.html` & `main.js`: 자바스크립트 실행을 위한 기본 셋팅이다. 코드는 여기에서 작성하고 검증 & 실험한다.
- `experiments/`: 공부 과정에서 생각난 것들을 실험하고 검증한 뒤, 내용을 기록하는 곳이다.
- `explanations/`: 정리된 내용이 있다면 여기서 정리한다.
- `projects/`: 최종적으로 공부한 내용과 연구, 이해 내용을 토대로 무언가를 만들어 보는 곳이다.
- `questions/`: 그날 나온 질문을 정리하는 곳이다.
- `trainings/`: 어떠한 개념을 학습했을 때, 코드를 작성할 때 자동적으로 생각할 수 있도록 반복 훈련하는 곳이다.
